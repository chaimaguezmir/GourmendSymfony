<form method="post" action="{{ path('app_restaurant_table_delete', {'id': restaurant_table.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restaurant_table.id) }}">
    <button class="btn">Delete</button>
</form>
